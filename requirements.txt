# DEPRECATED: this file is a thin shim, kept only for tooling that expects
# a requirements.txt (e.g. `pip install -r requirements.txt`).
#
# The single authoritative source of dependency metadata is pyproject.toml
# ([project].dependencies and [project.optional-dependencies].dev).
# Do not add version constraints here -- edit pyproject.toml instead.
-e .[dev]
